{
    "authentication": {
        "enabled": true,
        "types": [
            {
                "patern": "(?P<login>[a-zA-Z0-9@.]+):(?P<password>[a-zA-Z0-9@.]+)",
                "validation": "select * from account_passwords inner join users on account_passwords.user_id = users.id where users.email = $login$ and account_passwords.password = $password$",
                "roleId": "select role_id from roles inner join users on roles.user_id = users.id where users.email = $login$",
                "encoders": {
                    "password": "passKey"
                }
            }
        ]
    },

    "roles": {
        "0": {
            "value": 0,
            "name": "Admin"
        },
        "None": {
            "value": 2,
            "name": "Any role"
        }
    },

    "access": {
            "/users/login": {
            "minRoleId": 2
        }
    }
}